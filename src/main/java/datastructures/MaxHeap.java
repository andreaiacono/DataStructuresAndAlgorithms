package datastructures;

public class MaxHeap {

    /**
     * The max-heap property states that every node's value is greater than the value of
     * its children.
     * The max heap is a data structure ideal for implementing the abstract data structure
     * called priority queue.
     */
    int[] a;

    public MaxHeap(int size) {
        a = new int[size];
    }



}
